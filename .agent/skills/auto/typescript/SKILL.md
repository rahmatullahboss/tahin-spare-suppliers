---
name: typescript
description: "Typescript for tahin-spare-suppliers. 7 conventions."
domain: typescript
triggers:
  - glob: "**/*.ts"
  - glob: "**/*.tsx"
autoGenerated: true
enabled: true
---

# Typescript

Auto-compiled from **8 real patterns** in **tahin-spare-suppliers**. This skill is auto-routed to agents when working on typescript files.

## 📐 Conventions & Best Practices

### Project Conventions
- 📐 **convention in hospitalSite.ts** — File updated (external): src/routes/public/hospitalSite.ts

Content summary (239 lines):
import { Ho
- 📐 **Strengthened types Clone — offloads heavy computation off the main thread** — -     if (assetResponse.status !== 404) return assetResponse;
+     if (assetResponse.status !== 404
- 📐 **convention in patient-portal-journey.spec.ts** — -     
+     const errors: string[] = [];
-     // Check if bottom navigation is visible
+     page.
- 📐 **Strengthened types Health — formalizes the data contract with explicit types** — -     // ── Testimonials ──────────────────────────────────────────────────────
+     // ── My Healt
- 📐 **convention in patient-dashboard.spec.ts** — File updated (external): web/e2e/patient-dashboard.spec.ts

Content summary (406 lines):
import { te
- 📐 **Strengthened types Testimonials — formalizes the data contract with explicit ...** — -     // ── Hero ──────────────────────────────────────────────────────────────
+     // ── Testimon
- 📐 **Strengthened types Object — hardens HTTP security headers** — - export const languages = Object.keys({"en":{"nav.home":"Home","nav.biography":"Biography","nav.pub

## 🔵 Architecture

### how-it-works in i18n.ts
-     'pain.link': 'Health SaaS solves all three problems — in one system',
+     'pain.link': 'Ozzyl Health solves all three problems — in one system',
-     'sol.h2': 'Why is Health SaaS Different?'

---
*Auto-generated by BrainSync 🧠 | 8 patterns | 2026-05-05*
